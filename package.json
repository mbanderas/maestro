{
  "name": "maestro",
  "version": "1.2.0",
  "private": true,
  "description": "Maestro Frontier: orchestration discipline layer for AI coding agents + a zero-dependency local multi-CLI Frontier engine (off/single/fusion modes). Scripts wrap the ad-hoc node/npx checks.",
  "license": "MIT",
  "scripts": {
    "test": "node scripts/run-hook-tests.cjs",
    "lint": "npx --yes markdownlint-cli2",
    "bench-verify": "node scripts/bench-verify.cjs"
  }
}
