# shoplite commands

Run any command as `node src/cli.js <command>`.

| Command | Description |
|---|---|
| list-products | Print every product as `<id> <name> $<price>` |
| list-orders | Print every order as `<id> <customerId> $<total>` |
