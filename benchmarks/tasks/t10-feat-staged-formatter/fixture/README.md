# staged-formatter

A zero-dependency text table formatter for Node.js. Given a two-dimensional array of strings, it produces a plain-text table with columns aligned by their widest cell, optional per-column alignment (left or right), and an optional header separator line — all with no trailing whitespace on any line.
